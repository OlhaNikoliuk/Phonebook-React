import styled from '@emotion/styled/macro';

export const Header = styled.header`
  position: static;
  top: 0;
  left: 0;
  z-index: 10;
  display: flex;
  align-items: center;
  padding: 15px 40px;
  background: #283655;
  color: white;
`;
